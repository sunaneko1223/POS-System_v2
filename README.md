## 更新履歴

- **v2.2**
  - 会計処理をトランザクション化(保存失敗時に画面上とデータのズレが起きないように)
  - 受付番号の採番をDB(IndexedDB)のauto-increment機能任せに変更(複数タブ等での採番ズレ対策)
  - エラー発生時に自動でログをポップアップ表示する機能を追加(コピー可)
  - 商品管理画面の商品追加フォームに「種類」入力欄を追加
- **v2.1.1(HotFix)**
  - サーバー設定パネルを削除し、集計サーバーのURLは `data/serverlink.yml` のみを使う方式に変更
- **v2.1**
  - データベース名を変更(旧バージョンとのIndexedDBバージョン衝突を回避)
  - 会計確定ボタンの二重登録防止
  - `sw.js`のキャッシュ戦略を変更(`data/`配下はオンライン優先に)
  - 商品管理画面で追加した商品が、`item.yml`再読み込み後も消えないように修正
- **v2.0**
  - 配布用に全体をリニューアル(server統合、dexie.js採用、PWAアイコン修正 など)
